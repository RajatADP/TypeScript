export class Point6 {
    constructor(private x: number, private y: number) {
         }
 
     draw() {
         console.log('x: ' + this.x , ' y: ' + this.y);
     }
    }

    // adding export or import typescript things it as a module